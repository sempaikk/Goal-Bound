# Team pitch art

Place the field background here:

| File | Size |
|------|------|
| `pitch.jpg` | **820×1180** (portrait) |

`/team` uses this as the base layer, then composites player heads + name plates.

If missing, the bot falls back to the old SVG-drawn pitch.
